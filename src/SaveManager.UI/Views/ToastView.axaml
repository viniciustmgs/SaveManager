<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:SaveManager.UI.ViewModels"
             x:Class="SaveManager.UI.Views.ToastView"
             x:Name="Root"
             IsVisible="{Binding #Root.Toast.IsVisible}"
             IsHitTestVisible="False">

    <Grid>
        <!-- success toast -->
        <Border CornerRadius="8"
                Padding="16,10"
                MaxWidth="300"
                Background="#333333"
                BorderBrush="#4CAF50"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsVisible="{Binding #Root.Toast.IsSuccess}">
            <TextBlock Text="{Binding #Root.Toast.Message}"
                       Foreground="White"
                       TextWrapping="Wrap"
                       TextAlignment="Center" />
        </Border>

        <!-- error toast -->
        <Border CornerRadius="8"
                Padding="16,10"
                MaxWidth="300"
                Background="#333333"
                BorderBrush="#F44336"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsVisible="{Binding !#Root.Toast.IsSuccess}">
            <TextBlock Text="{Binding #Root.Toast.Message}"
                       Foreground="White"
                       TextWrapping="Wrap"
                       TextAlignment="Center" />
        </Border>
    </Grid>
</UserControl>